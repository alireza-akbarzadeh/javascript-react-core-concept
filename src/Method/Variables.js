// Assign the string value Sammy to the username identifier
var username = "sammy_shark";
// Check if variable is equal to value
if (username === "sammy_shark") {
  //   console.log(true);
}
// Output true

// Assignment of various variables
var name = "Sammy";
var spartans = 300;
var kingdoms = ["mammals", "birds", "fish"];
var poem = { roses: "red", violets: "blue" };
var success = true;
var nothing = null;

// Assign value to password variable
var password = "hunter2";
var password = "mamd";
// Reassign variable value with a new value
password = "hunter3";
// Output 'hunter3'

let user = "ali";
user = 132;
user = null;
user = undefined;
export { password, user };

// Difference Between var, let, and const
// JavaScript has three different keywords
//  to declare a variable, which adds an extra layer of
//  intricacy to the language. The differences between the three
// are based on scope, hoisting, and reassignment.

// Initialize a global variable
var creature = "wolf";

// Attempt to use a variable before declaring it
// console.log(x);

// Variable assignment without var
// x = 100;
